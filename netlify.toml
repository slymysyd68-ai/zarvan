[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links like /products/saffron-sargol-negin don't 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
